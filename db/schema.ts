import {sqliteTable,text,integer,real,index} from 'drizzle-orm/sqlite-core';
export const profiles=sqliteTable('profiles',{userId:text('user_id').primaryKey(),name:text('name').notNull(),email:text('email').notNull(),updatedAt:text('updated_at').notNull()});
export const runs=sqliteTable('runs',{id:text('id').primaryKey(),userId:text('user_id').notNull(),unit:integer('unit').notNull(),mode:text('mode').notNull(),questionIds:text('question_ids').notNull(),answers:text('answers'),score:integer('score'),writing:text('writing'),feedback:text('feedback'),rubric:text('rubric'),teacherComment:text('teacher_comment'),voiceKey:text('voice_key'),voiceMime:text('voice_mime'),voiceTranscript:text('voice_transcript'),voiceScore:real('voice_score'),voiceDuration:integer('voice_duration'),createdAt:text('created_at').notNull(),completedAt:text('completed_at')},t=>[index('idx_runs_user_completed').on(t.userId,t.completedAt),index('idx_runs_completed').on(t.completedAt)]);
